---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tm-tls-secret
  namespace: textmanager
spec:
  secretName: tm-tls-secret
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - tm.terzeron.com
    - api-tm.terzeron.com
